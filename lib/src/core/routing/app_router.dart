import 'package:go_router/go_router.dart';
class AppRouter{
  GoRouter getConfig(){
    /// The route configuration.
    final GoRouter router = GoRouter(
      routes: <RouteBase>[

      ],
    );
    return router;
  }
}