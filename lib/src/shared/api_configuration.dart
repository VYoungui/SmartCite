
//final String baseUrl = dotenv.env['BASE_URL']!;
//const String baseUrl = 'https://mailexapi-assqzdkzla-uc.a.run.app/api/v1';

/*
enum EndPoint {
  category,
  auth,
  cote,
  mail,
  department,
  entreprise,
  function,
  response,
  user
}

 */

/*
final endpointValues = EnumValues({
  'categorie': EndPoint.category,
  'auth': EndPoint.auth,
  'cote': EndPoint.cote,
  'courrier': EndPoint.mail,
  'departement': EndPoint.department,
  'entreprise': EndPoint.entreprise,
  'fonction': EndPoint.function,
  'reponse': EndPoint.response,
  'utilisateur': EndPoint.user,
});

 */
/*
class EnumValues<T> {
  Map<String, T> map;
  late Map<T, String> reverseMap;

  EnumValues(this.map);

  Map<T, String> get reverse {
    reverseMap = map.map((k, v) => MapEntry(v, k));
    return reverseMap;
  }
}

 */